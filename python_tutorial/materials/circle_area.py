# Ask for the circle radius from the user.
input_radius = input("Give the circle radius (must be integer): ")

# Convert user's input to an integer.
input_radius = int(input_radius)

# Calculate the circle area.
area = input_radius**2 * 3.14

# Print the circle area out.
print(area)
